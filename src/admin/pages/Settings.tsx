import React from 'react';
import PageHeader from '../components/PageHeader';

const Settings: React.FC = () => {
  return (
    <div>
      <PageHeader title="Settings" description="Admin settings will be available here." />
    </div>
  );
};

export default Settings;
